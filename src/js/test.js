function testword() {
	let argArr = Array.from(arguments)
	let newArr = []
	argArr.forEach((el) => {
		newArr.push(el.split(''))
	})
	newArr.forEach((el, i) => {
		el.forEach()
	})
}

testword('кот', 'ток')